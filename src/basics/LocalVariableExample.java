package basics;

public class LocalVariableExample {
    public static void main(String[] args){  
         /*In the given code, a and b are both local variables of the main method, 
    which means that they are only accessible within that method.
Therefore, a and b are local variables of the main method, and they are of the integer type (int).
The variable c is also a local variable of the main method and is of the integer type (int).
So in summary, all variables in the given code are local variables of the main method and are of the integer type (int). */  
        int a=10;    
        int b=10;    
        int c=a+b;    
        System.out.println(c);    
        }  
    
}
